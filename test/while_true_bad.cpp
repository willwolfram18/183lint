using namespace std;

int main () {
    // while true
    while(true) {
        int y = 0;
        if (y) {
            
        }
    }
    while ( true ) {}
    do {
        int y = 0;
        if (y) {
            
        }
    } while (true);
    
    // while !false
    while(!false) { }
    while ( !false ) { }
    do {
    } while (!false);
    
    // while 1
    // while (1) {}
    // while ( 1 ) {}
    // do {
    // } while(1);
    
    // while !0
    // while (!0) {}
    // while ( !0 ) {}
    // do {
    // } while(!0);

    // using comparisons
    int x = 0;
    while (x != 1) {}
    for (int i = 0; true;) {}
    for (int i = 0; x != 1; ){}
}
